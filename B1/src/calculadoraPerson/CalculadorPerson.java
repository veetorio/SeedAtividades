package calculadoraPerson;

public class CalculadorPerson {
    public CalculadorPerson(int num){
        for (int i = 1; i <= 10 ; i++) {
            System.out.println( i + " * " + num + " = " + i * num);
        }
    }
}
